# 交接记录 2026-03-26

## 当前目标

把项目从旧的 `html/css/js + Python http.server` 逐步迁到这套：

- 前端：`React + Vite`
- 图表：`TradingView Lightweight Charts`
- 后端：`FastAPI`
- 数据：`SQLite`

同时把数据源接口做成模块化，业务层不能继续直接绑死 `Tushare`，方便后续切换别的行情供应商。

用户已经明确确认这条路线，不要再继续把主要精力放在旧静态页修补上。

## 今天已经完成的事

### 1. 自选股 SQLite 持久化

已新增：

- [backend/watchlist_store.py](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/watchlist_store.py)

已接入旧后端接口：

- `GET /api/watchlist`
- `POST /api/watchlist`
- `PUT /api/watchlist/<tsCode>`
- `DELETE /api/watchlist/<tsCode>`

数据库文件：

- [backend/data/watchlist.db](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/data/watchlist.db)

### 2. 本地行情缓存 SQLite

已新增：

- [backend/market_data_store.py](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/market_data_store.py)

数据库文件：

- [backend/data/market_cache.db](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/data/market_cache.db)

当前已缓存内容：

- `stock_snapshot` 有 22 天
- `sector_snapshot` 有 3 天

已知数据库表：

- `frame_cache`

### 3. MarketEngine 已开始接本地缓存

已修改：

- [backend/market_engine.py](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/market_engine.py)

已经做过的方向：

- `stock_snapshot()` 优先读 SQLite
- `sector_snapshot()` 优先读 SQLite
- `latest_data_trade_date()` 尝试基于本地缓存退化
- `trade_dates()` 联网失败时尝试基于本地 `stock_snapshot` 日期退化
- `stock_name_map()` / `sector_name_map()` / `sw_l3_map()` 联网失败时不再直接炸掉
- `market_overview()` / `sector_rankings()` 尝试避免直接抛 500

注意：

- 这部分代码今天刚补离线退化，还没有完成整轮联调。
- 下一次继续前，先跑接口验证，不要默认它已经完全稳定。

### 4. 旧前端已经不再是主方向

旧页面仍在：

- [web/index.html](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/web/index.html)
- [web/app.js](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/web/app.js)
- [web/styles.css](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/web/styles.css)

旧页上做过一些功能：

- 自选卡片
- 细分编辑
- 单卡多图布局
- 多列展示

但用户后面明确要求上成熟框架，因此旧页只保留作临时回退，不再做主开发。

### 5. React + Vite 前端骨架已建立

已新增：

- [frontend/package.json](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/frontend/package.json)
- [frontend/src/App.jsx](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/frontend/src/App.jsx)
- [frontend/src/main.jsx](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/frontend/src/main.jsx)
- [frontend/src/styles.css](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/frontend/src/styles.css)
- [frontend/vite.config.js](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/frontend/vite.config.js)

当前 React 版只是一个试跑骨架：

- 可拉 `/api/market/overview`
- 可拉 `/api/watchlist`
- 可拉 `/api/sectors/rankings`
- 还没有迁入 `Lightweight Charts`
- 还没有迁出完整“我的自选”卡片图表

## 当前线上实际问题

### 主问题

旧后端 `8080` 的 `500` 根因不是前端，而是：

- 当前运行环境访问 `http://139.196.25.182` 不稳定
- `trade_cal` / `ths_index` 等在线接口失败
- 然后旧 `server.py` 把异常统一吞成 `{"error": "服务器内部错误"}`

已确认过的异常链路：

- `requests.exceptions.ConnectionError`
- `urllib3.exceptions.MaxRetryError`
- 调用目标：`139.196.25.182/trade_cal`

### 关键结论

不要再把这类报错当成页面问题。

先分清三类：

- 前端渲染问题
- 后端代码问题
- 外部依赖问题：`Tushare / token / 代理 / 网络`

## 用户明确提出过的要求

后续必须遵守：

- “开始不要问，最优解”
- “每次错误需记录下次不要重复错”

执行要求：

- 先看日志再判断，不靠猜
- 同类错误修一次，不要重复走同样坏路径
- 像 `favicon.ico 404` 这种噪音不要再当主问题
- 修完后先打接口验证，再让用户刷新

## 明天继续时的最短路径

### 第一优先级

把 `FastAPI` 骨架搭起来，复用现有业务层，不要继续扩大旧 `server.py` 的修改面。

建议新增：

- `backend/api_app.py`

并且同步做数据源抽象层，建议新增：

- `backend/providers/base.py`
- `backend/providers/tushare_provider.py`
- `backend/providers/__init__.py`

先平移这些接口：

- `GET /api/watchlist`
- `POST /api/watchlist`
- `PUT /api/watchlist/{ts_code}`
- `DELETE /api/watchlist/{ts_code}`
- `GET /api/market/overview`
- `GET /api/sectors/rankings`

原则：

- 直接复用 `MarketEngine`
- 直接复用 `WatchlistStore`
- 接口失败时返回明确错误，不要继续全部包成笼统 `500`

### 数据源模块化要求

这是用户明确要求，后面必须按这个结构走。

目标：

- `MarketEngine` 依赖抽象数据接口，不直接依赖 `ts.pro_api`
- `Tushare` 只是其中一个 provider 实现
- 后面如果改成别的供应商，只换 provider，不改业务规则和前端接口

建议接口最少覆盖这些能力：

- `trade_dates(end_date, lookback_days)`
- `stock_snapshot(trade_date)`
- `sector_snapshot(trade_date)`
- `stock_name_map()`
- `sector_meta()`
- `sw_l3_map()`
- `sector_members(sector_code, trade_date)`
- `stock_daily_bars(ts_code, start_date, end_date)`
- `sector_daily_bars(sector_code, start_date, end_date)`

建议分层：

- provider：只负责拉原始行情数据
- store：只负责 SQLite 缓存
- engine：只负责筛选规则、RPS、MA、市场状态等业务计算

不要再让 `MarketEngine` 里面直接出现大量 `self.pro.xxx()`，这会把切源成本做高。

### 第二优先级

React 版先完成一个可用页面，不追求全迁完。

顺序建议：

1. 顶部入口做“我的自选”
2. 自选页一行四卡
3. 每张卡片内：
   - 股票名
   - 细分，可编辑
   - 一个整体图框
   - 图框内三 pane：
     - 主图：裸 K
     - 副图1：成交额
     - 副图2：个股 RPS + 板块 RPS

对应技术：

- `lightweight-charts`

### 第三优先级

把旧 `app.js` 里的数据逻辑拆到 React 里，不要原样复制 DOM 操作。

建议先抽：

- `frontend/src/lib/api.js`
- `frontend/src/components/WatchlistCard.jsx`
- `frontend/src/components/WatchlistChart.jsx`
- `frontend/src/components/SectorRankingTable.jsx`

## 明天一开始建议执行的命令

先看当前端口和服务状态：

```bash
lsof -nP -iTCP:8080 -sTCP:LISTEN
lsof -nP -iTCP:5173 -sTCP:LISTEN
```

再看旧后端接口是否还在报错：

```bash
env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY \
curl -s -i --max-time 20 'http://127.0.0.1:8080/api/market/overview?tradeDate='
```

再开始 FastAPI 迁移。

## 当前文件中最值得优先看的地方

- [backend/market_engine.py](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/market_engine.py)
- [backend/watchlist_store.py](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/watchlist_store.py)
- [backend/market_data_store.py](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/backend/market_data_store.py)
- [frontend/src/App.jsx](/Users/yiming/Library/Mobile Documents/com~apple~CloudDocs/AI项目/A数据/frontend/src/App.jsx)

## 这次停下前的状态

- 旧后端 `8080` 仍在运行，但可能继续因外部接口失败而不稳定
- React/Vite `5173` 骨架已建立
- `FastAPI` 还没开始落文件
- `Lightweight Charts` 还没安装到 `frontend/`

## 不要做的事

- 不要继续在旧静态页上大规模堆功能
- 不要再靠猜测解释 `500`
- 不要把外部依赖报错伪装成“页面没东西”
- 不要改动用户未要求清理的其他脏工作区内容
