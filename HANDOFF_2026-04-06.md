# 2026-04-06 进度记录

## 今日已完成

### 1. 盘前纪要 404 修复
- **根因**: 前端 `/api/market/recap` 与后端 `/api/market-recap` 路径不匹配
- **修复**: 改前端 `market-recap.service.ts` 路径
- 验证: 51 涨停、11 新高、20 预警正常返回

### 2. 股票搜索 2.5s → 22ms
- **根因**: `search_market` 每次实时计算 `compute_stock_metrics` + 逐股调 `ths_member` API
- **方案**: 三层缓存
  1. **预计算层**: `precompute.py` 新增 `search_snapshot` 表（5424 股票全量指标+板块映射）
  2. **内存映射**: `_get_stock_sector_map()` 反转 ths_member 缓存为 `stock→sector` dict
  3. **板块轻量化**: 搜索板块只查内存名称映射，不调 `sector_rankings`
- 首次搜索缓存后，后续 22ms

### 3. 股票对比页崩溃修复
- **根因**: `error-tracking.ts` 缺少 `useCallback` import
- **根因2**: `stock-selector.tsx` 的 `rps10.toFixed()` / `rps20.toFixed()` 未做空值保护
- **修复**: 添加 import + `?? 0` 保护

### 4. 股票对比选中持久化
- 使用 zustand `persist` 中间件 + `localStorage`
- 已选股票刷新页面不丢失，除非手动删除

### 5. NaN 数据修复
- `market_recap` 返回的 hotMoney/institutional 数据中 NaN → 0
- 涉及: `_recap_hot_money`, `_recap_institutional` 的 `buy/sell/net_buy` 字段

### 6. 左侧导航 hover tooltip 移除
- `left-rail.tsx` 去掉 `Tooltip`/`TooltipTrigger`/`TooltipContent`

### 7. K 线接口缓存
- `stock_kline_ashare`: 重构缩进+加 `_cached` 300s TTL
- `stock_kline`: 加 `_cached` 300s TTL（原来无缓存）
- 首次 ~4s，缓存后 <10ms

### 8. 开发工作流文档
- 创建 `docs/development-workflow.md`
- 记录三层缓存架构、性能基准、开发流程

## 提交记录
- `7442786` fix: market-recap 404, search rps10 crash, nav tooltips, add precompute search snapshot
- `032aef8` feat: search 22ms cache, stock-compare persistence, NaN fixes, workflow docs
- `c1b3009` feat: K-line 5min cache, stock-compare localStorage, error-tracking fix (含前一次)

## 未完成

| 功能 | 状态 | 说明 |
|------|------|------|
| 画线改颜色 | 待做 | `drawing-toolbar.tsx` |
| 双底扫描超时 | 待排查 | `/api/hh-scan` 接口卡住 |
| AI 研究助手 | 待确认 | 功能未定义 |
| DeerFlow 集成 | 待做 | `deer-flow-service` 目录不存在 |

## 启动方式
```bash
bash dev.sh          # 前端 5174 / 后端 8088
bash dev.sh stop     # 停止
bash dev.sh status   # 查看状态
```
