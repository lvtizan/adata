# 2026-04-03 进度记录

## 当前结论

- 正确的本地开发流程是 `bash dev.sh`
- 正确端口是：
  - 前端 `5174`
  - 后端 `8088`
- 之前出现的 `5173 / 8080` 属于旧流程或手动启动方式，已经确认不应继续使用

## 今天已完成

### 1. 盘中观察恢复主工作区

- [frontend/src/pages/intraday/page.tsx](/Users/yiming/Code/A数据/frontend/src/pages/intraday/page.tsx)
  - 主区域恢复为三列：
    - 概念板块
    - 板块个股
    - 个股 K 线
  - 今晚新增内容不再占主工作区，改为右侧独立侧栏 `市场总览`

### 2. 新增独立入口“市场总览”

- [frontend/src/pages/market-overview/page.tsx](/Users/yiming/Code/A数据/frontend/src/pages/market-overview/page.tsx)
  - 新建独立页面
  - 使用“顶部环境条 + 左侧板块 + 中间评分表 + 右侧研究详情”的布局

- [frontend/src/app/router/routes.tsx](/Users/yiming/Code/A数据/frontend/src/app/router/routes.tsx)
  - 新增 `/market-overview`

- [frontend/src/app/layouts/root-layout.tsx](/Users/yiming/Code/A数据/frontend/src/app/layouts/root-layout.tsx)
  - 左侧菜单新增“市场总览”入口

### 3. 新增 intraday 评分与研究组件

- [frontend/src/features/intraday/index.ts](/Users/yiming/Code/A数据/frontend/src/features/intraday/index.ts)
- [frontend/src/features/intraday/lib/score.ts](/Users/yiming/Code/A数据/frontend/src/features/intraday/lib/score.ts)
- [frontend/src/features/intraday/market-context-bar.tsx](/Users/yiming/Code/A数据/frontend/src/features/intraday/market-context-bar.tsx)
- [frontend/src/features/intraday/stock-research-panel.tsx](/Users/yiming/Code/A数据/frontend/src/features/intraday/stock-research-panel.tsx)
- [frontend/src/features/intraday/types.ts](/Users/yiming/Code/A数据/frontend/src/features/intraday/types.ts)

功能状态：
- 已可计算总分、评级、子分、标签、结论
- 已能在“市场总览”页面和“盘中观察”右侧栏展示
- 已加环境分悬浮拆解和独立研究面板骨架

### 4. 启动流程已统一

以下旧入口已统一转发到 `dev.sh`，避免再次跑出错端口：

- [dev.sh](/Users/yiming/Code/A数据/dev.sh)
- [start.sh](/Users/yiming/Code/A数据/start.sh)
- [stop.sh](/Users/yiming/Code/A数据/stop.sh)
- [start_server.sh](/Users/yiming/Code/A数据/start_server.sh)
- [dev.command](/Users/yiming/Code/A数据/dev.command)
- [start.command](/Users/yiming/Code/A数据/start.command)
- [stop.command](/Users/yiming/Code/A数据/stop.command)
- [run_index.command](/Users/yiming/Code/A数据/run_index.command)
- [run_index_no_proxy.command](/Users/yiming/Code/A数据/run_index_no_proxy.command)
- [setup.sh](/Users/yiming/Code/A数据/setup.sh)

统一后约定：

```bash
bash dev.sh
bash dev.sh stop
bash dev.sh status
```

### 5. 资源 404 已处理

- [frontend/favicon.svg](/Users/yiming/Code/A数据/frontend/favicon.svg)
  - 已新增，修复浏览器 `favicon` 404

## 已验证

- `cd frontend && npm run check` 通过
- `cd frontend && npm run build` 通过

注意：
- 这些验证只说明前端代码可编译
- 不代表市场数据接口当前一定能立即返回

## 当前未解决的关键问题

### 市场数据接口仍不稳定

当前最主要的 blocker 不是页面，而是后端拉市场数据时被代理/上游超时拖住。

已观察到：

- `/api/watchlist` 正常返回
- `/api/status` 正常返回
- 但下面这些接口会卡很久甚至空白：
  - `/api/market/overview`
  - `/api/sectors/rankings`
  - `/api/intraday/sectors`

后端日志明确出现：

```text
HTTPConnectionPool(host='127.0.0.1', port=1082): Read timed out
```

以及：

- 交易日历接口失败
- 股票名称映射加载失败
- 板块名称映射加载失败
- 指数分析失败

这说明：

- 前后端联通没问题
- 真正卡的是市场数据源调用
- 当前进程在访问外部数据时被一个失效代理 `127.0.0.1:1082` 拖住

## 明天第一优先级

### 先恢复“数据正常展示”

优先处理后端：

1. 检查 `backend/market_engine.py` 里 Tushare 请求链路
2. 找出为什么会走到 `127.0.0.1:1082`
3. 去掉这个失效代理影响
4. 对这些函数加更快的 fallback，避免整页等 30 秒：
   - `trade_dates`
   - `latest_data_trade_date`
   - `stock_name_map`
   - `sector_name_map`
   - `market_overview`
   - `sector_rankings`
   - `intraday_sector_rankings`

目标是：

- 页面至少先显示本地快照数据
- 不要因为代理超时让整个页面空白

## UI/产品上已讨论但未落地

以下内容聊过，但还没做完：

- 系统品牌名改成 `AI学习系统`
- loading 改成单一转圈，不要假进度条
- 股票上增加“多维打星评分卡”
- K 线上加画中画评分卡

这些都排在“先恢复数据展示”之后。

## 当前建议的启动方式

明天继续时，只使用：

```bash
bash dev.sh
```

然后打开：

```text
http://127.0.0.1:5174/
```

不要再用：

- `5173`
- `8080`
- 单独 `npm run dev`
- 单独 `python3 backend/server.py`

## 备注

- 今天有多次“服务看起来启动成功，但端口后续又不可访问”的现象
- 这和当前工具环境里的后台进程保持方式有关，所以明天最好直接在你自己的终端里运行 `bash dev.sh`
- 但即使进程稳定，市场数据本身仍会被 `127.0.0.1:1082` 代理超时拖住，这才是业务数据空白的核心问题
