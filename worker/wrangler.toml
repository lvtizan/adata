name = "a-data-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# D1 数据库绑定 — 创建后填入 database_id
[[d1_databases]]
binding = "DB"
database_name = "a-data-db"
database_id = "bf5595a0-d052-4cbf-ba33-4080c8b6e65c"

# KV 命名空间绑定 — 创建后填入 id
# 取消下面注释并填入真实 ID:
# [[kv_namespaces]]
# binding = "KV"
# id = "" # TODO: wrangler kv namespace create a-data-quotes 后填入

[vars]
# ACCESS_TOKEN 通过 wrangler secret put 设置，不要写在这里
# 本地开发用: wrangler dev 时会提示输入
