# 交接文档 — 2026-03-28

## 今日完成

### 1. 板块排名升降指标 ✅
- **后端** `market_engine.py` → `sector_rankings()` 新增 `rankChange` / `prevRank` 字段（对比昨日排名）
- **前端** `App.jsx` 板块列表第 1 列显示 ↑N / ↓N / - 变化标识
- **CSS** 新增 `.rank-change` / `.up` / `.down` / `.flat` 样式

### 2. 财务数据 API ✅
- **后端** `market_engine.py` 新增 `stock_financials(ts_code)` 方法
  - 拉取 Tushare `income` + `fina_indicator` 表，最近 8 个季度
  - 返回 `quarters[]` 数组，每项含 `revenue`, `netIncome`, `grossMargin`, `roe`, `revenueYoY`, `netIncomeYoY`, `endDate`
- **路由** `api_app.py` → `GET /api/stock/{ts_code}/financials`

### 3. MCP Server（DeerFlow 集成）✅
- **文件** `backend/mcp_server.py`
- 7 个工具：`astock_market_overview`, `astock_sector_rankings`, `astock_sector_stocks`, `astock_bull_camp`, `astock_stock_kline`, `astock_stock_financials`, `astock_relative_strength`
- 支持 stdio（本地 DeerFlow 调用）和 streamable-http（端口 8083）两种传输模式
- 配置示例：`deerflow/extensions_config_example.json`

### 4. DeerFlow 工作流设计 ✅
- `deerflow/skill_stock_research.md` — 个股深度研究 Skill
- `deerflow/PLAYBOOK.md` — 9 大炒股场景方案
- `deerflow/README.md` — 集成指南

### 5. 牛股集中营 v2 信息架构设计 ✅
- 设计文档：`docs/plans/2026-03-28-bullcamp-v2-design.md`
- 三 Tab 架构：图表 / 财务 / 资讯

### 6. 牛股集中营 v2 — P0 前端实现 ✅
- **Tab 框架**：`BullCampWorkbench.jsx` 重写
  - 三个 Tab 按钮：图表（默认）、财务、资讯
  - Tab 切换用 `useState`，懒加载各 Tab 内容
  - 资讯 Tab 暂为占位说明
- **财务面板**：新建 `FinancialsPanel.jsx`
  - 4 个 KPI 卡片：最新营收、最新净利、毛利率、ROE（含同比/环比变化）
  - 季度明细表格：报告期、营收、同比、净利润、同比、毛利率、ROE
  - 金额自动转换为"亿"单位
  - 从 `/api/stock/{ts_code}/financials` 接口获取数据
- **左侧列表状态标签**：`CampTag` 组件
  - `新` — 今天新进入集中营（红底白字）
  - `ND` — 连续 N 天在集中营（蓝色标签，≥3 天才显示）
  - `财` — 近 7 天有新财报公告（黄色标签）
  - 注意：标签依赖后端返回 `isNew`, `daysInCamp`, `hasRecentAnnouncement` 字段（P0 后端待实现）
- **CSS** 新增：`.camp-tab-*`, `.fin-kpi-*`, `.camp-tag-*`, `.camp-news-*` 等样式
- **API** `lib/api.js` 新增 `getStockFinancials(tsCode)` 函数

---

## 未完成 / 待做

### P0 后端（标签数据）
- [ ] `bull_camp()` 返回数据增加 `isNew`, `daysInCamp`, `hasRecentAnnouncement` 字段
- [ ] 需新接口 `/api/camp/bull-stocks/history` 获取集中营历史列表，用于计算连续在营天数

### P1（需少量后端改动）
- [ ] 财务 Tab 营收趋势迷你图（HistogramSeries 柱状图，8 个季度）
- [ ] 资讯 Tab 模式 A：公告/新闻列表（Tushare `news`/`anns` 接口）
- [ ] 左侧列表迷你趋势 sparkline（最近 5 天 campScore 变化）

### P2（需 DeerFlow 集成）
- [ ] 资讯 Tab 模式 B：AI 研究简报（"生成研究简报"按钮 → DeerFlow）
- [ ] 一句话摘要缓存
- [ ] 信息卡头部增强（快速操作按钮、DeerFlow 摘要）

### Git
- [ ] 最新改动尚未 commit（包括 P0 前端代码、MCP server、DeerFlow 文件、排名升降等）

### 基础设施
- [ ] `setup_local_deps.sh` 尚未在本地运行（将 node_modules 和 data 用 symlink 移出 iCloud）
- [ ] 前端 `npm install` 需在本地执行（sandbox 无网络权限）

---

## 文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/market_engine.py` | 修改 | 新增 `stock_financials()`、`sector_rankings()` 加 `rankChange` |
| `backend/api_app.py` | 修改 | 新增 `/api/stock/{ts_code}/financials` 路由 |
| `backend/mcp_server.py` | 新增 | DeerFlow MCP Server（7 个工具） |
| `frontend/src/lib/api.js` | 修改 | 新增 `getStockFinancials()` |
| `frontend/src/components/BullCampWorkbench.jsx` | 重写 | Tab 框架 + CampTag 标签 |
| `frontend/src/components/FinancialsPanel.jsx` | 新增 | 财务面板（KPI 卡 + 表格） |
| `frontend/src/components/index.js` | 修改 | 导出 `FinancialsPanel` |
| `frontend/src/styles.css` | 修改 | 新增 Tab / 财务 / 标签 / 占位 CSS |
| `frontend/src/App.jsx` | 修改 | 板块排名升降 ↑↓ 显示 |
| `deerflow/skill_stock_research.md` | 新增 | 个股深研 Skill |
| `deerflow/PLAYBOOK.md` | 新增 | 9 大场景方案 |
| `deerflow/README.md` | 新增 | 集成指南 |
| `deerflow/extensions_config_example.json` | 新增 | DeerFlow MCP 配置示例 |
| `docs/plans/2026-03-28-bullcamp-v2-design.md` | 新增 | v2 信息架构设计 |
| `setup_local_deps.sh` | 新增 | 本地依赖 symlink 脚本 |

---

## 启动方式

```bash
# 后端
cd ~/Library/Mobile\ Documents/com~apple~CloudDocs/AI项目\ 2/A数据/backend
python3 api_app.py

# 前端
cd ~/Library/Mobile\ Documents/com~apple~CloudDocs/AI项目\ 2/A数据/frontend
npm run dev

# MCP Server（可选，给 DeerFlow 用）
python3 backend/mcp_server.py --http  # 端口 8083
```

---

## 注意事项

1. 项目路径已改为 `~/Library/Mobile Documents/com~apple~CloudDocs/AI项目 2/A数据/`
2. `WatchlistChart.jsx` 已被外部（用户/linter）大幅改写，加入了 `lightweight-charts-drawing` 画线工具、localStorage 持久化、第三个 RPS 对比图表。`autoSize: true` 修复保留。
3. `api_app.py` 外部另加了一个 `/api/bullcamp` 路由（与原有 `/api/camp/bull-stocks` 功能重复），注意后续清理。
4. 财务面板的 CampTag 标签已在前端实现，但后端 `bull_camp()` 尚未返回 `isNew`/`daysInCamp`/`hasRecentAnnouncement` 字段，这是下一步的后端工作。
