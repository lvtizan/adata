# 交接文档 — 2026-03-28 v2（累计更新）

## 本轮完成

### 1. 形态识别系统（pattern_detector.py）✅
- **5 种形态**：杯柄、VCP、紧凑盘整、三线开花、口袋支点
- **杯柄特殊逻辑**：左侧 30%+ 连续拉升 → 回调 10-40% → U 型底部 → HH 信号
- **MA 多头排列检测**：MA30 > MA50 > MA200，价格在三线之上
- **HH/HL 信号点**：返回日期和价格坐标，供 K 线图标记
- **单元测试**：18 个测试用例（`backend/tests/test_pattern_detector.py`），全部通过

### 2. K 线图信号叠加 ✅
- `watchlist-chart.tsx` 接入 `useStockPatterns` 查询
- HH 信号显示为琥珀色向上箭头，HL 为绿色向上箭头
- MA 多头排列时显示 "MA多头排列" 徽章
- 使用 `candleSeries.setMarkers()` API

### 3. 牛股集中营列表增强 ✅
- **形态标签**（camp-tag.tsx）：杯柄/VCP/紧凑/三线开花/口袋，各有独立颜色
- **campScore 趋势 sparkline**（score-sparkline.tsx）：最近 5 天综合分变化迷你折线图
- 后端 `bull_camp()` 新增 `campScoreHistory` 字段（复用 history 缓存，零额外 API 调用）

### 4. 资讯 Tab ✅
- `news-panel.tsx`：显示个股公告列表，含日期和链接
- 后端 `GET /api/stock/{ts_code}/news` 接口

### 5. 财务 Tab 增强 ✅
- SVG 营收趋势迷你图（8 季度营收/净利润柱状图）
- 已嵌入 FinancialsPanel 组件

### 6. 开发工作流完善 ✅
- SKILL.md 新增「一致性是过程约束，不是事后检查」原则
- 6 步自检工作流 + 验证脚本 `validate_project.py`
- API 参考文档 `references/api-endpoints.md`
- 文件树快照 `references/file-tree.md`

### 7. Settings 页面路由修复 ✅
- `routes.tsx` + `root-layout.tsx` navItems 同步更新

### 8. Tushare Skill 配置 ✅
- `tushare/SKILL.md` 和 `tushare-data/SKILL.md` 代理地址统一为 `http://139.196.25.182`
- Token 配置完整
- 清理了 `tushare-official` 残留目录

---

## 新增/修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/pattern_detector.py` | 新增 | 5 种形态检测 + HH/HL 信号 |
| `backend/market_engine.py` | 修改 | `_detect_patterns_batch`, `_bull_camp_score_history`, `bull_camp()` 增强 |
| `backend/server.py` | 修改 | `/api/stock/{ts_code}/patterns`, `/api/stock/{ts_code}/news`, `/api/camp/bull-stocks/history` |
| `backend/tests/test_pattern_detector.py` | 新增 | 18 个单元测试 |
| `frontend/src/features/bullcamp/components/camp-tag.tsx` | 修改 | 形态标签渲染 |
| `frontend/src/features/bullcamp/components/score-sparkline.tsx` | 新增 | campScore 迷你趋势图 |
| `frontend/src/features/bullcamp/components/news-panel.tsx` | 新增 | 公告列表面板 |
| `frontend/src/features/bullcamp/components/financials-panel.tsx` | 修改 | SVG 营收趋势图 |
| `frontend/src/features/watchlist/components/watchlist-chart.tsx` | 修改 | HH/HL markers + MA徽章 |
| `frontend/src/shared/charts/kline-chart.tsx` | 修改 | ChartSignal interface + marker 渲染 |
| `frontend/src/services/stock.service.ts` | 修改 | `getStockPatterns`, `getStockNews` |
| `frontend/src/queries/stock.queries.ts` | 修改 | `useStockPatterns`, `useStockNews` |
| `frontend/src/shared/types/stock.ts` | 修改 | `patternTags`, `campScoreHistory`, `StockNewsItem` |
| `frontend/src/pages/bullcamp/page.tsx` | 修改 | NewsPanel + CampTag + Sparkline 集成 |
| `frontend/src/app/router/routes.tsx` | 修改 | Settings 路由 |
| `frontend/src/app/layouts/root-layout.tsx` | 修改 | Settings 导航项 |
| `frontend/src/tsconfig.app.json` | 修改 | `ignoreDeprecations: "6.0"` |
| `.claude/skills/a-stock-dev/SKILL.md` | 修改 | 一致性原则 + 自检工作流 |
| `.claude/skills/a-stock-dev/references/api-endpoints.md` | 新增 | API 参考 |
| `.claude/skills/a-stock-dev/references/file-tree.md` | 新增 | 文件树 |
| `.claude/skills/tushare/SKILL.md` | 修改 | 代理地址 → 139.196.25.182 |
| `.claude/skills/tushare-data/SKILL.md` | 修改 | 代理地址 → 139.196.25.182 |
| `scripts/validate_project.py` | 新增 | 项目验证脚本 |

---

## 未完成 / 待做

### P1
- [ ] 左侧列表迷你趋势 sparkline — 5天 campScore 变化（**后端已完成，前端已完成，待本机验证**）

### P2（需 DeerFlow 集成）
- [ ] 资讯 Tab 模式 B：AI 研究简报（"生成研究简报"按钮 → DeerFlow）
- [ ] 一句话摘要缓存
- [ ] 信息卡头部增强（快速操作按钮、DeerFlow 摘要）

### 本机验证
- [ ] `./dev.command` 启动全栈
- [ ] `cd frontend && npx tsc --noEmit` 验证 TypeScript 编译
- [ ] 浏览器访问 `http://localhost:5173/bullcamp` 检查 sparkline 和形态标签显示
- [ ] Git commit

---

## 启动方式

```bash
cd /Users/kp/Code/A数据 && ./dev.command
```

## 注意事项

1. 项目路径：`/Users/kp/Code/A数据/`
2. Tushare 代理必须设置 `pro._DataApi__http_url = 'http://139.196.25.182'`
3. node_modules 通过 symlink 放在 `~/.cache/a-data-local/`，避免 iCloud 同步
4. 沙箱无法运行 tsc / vite build（macOS native binaries），TypeScript 检查需在本机执行
